import {Button} from "@mui/material";

const UserProfile = () => {

  return <div style={{
    minHeight: '100vh',
    textAlign: 'center',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: '20px'
  }}>
    <div>开发中，别催....</div>
    <Button href={'/'}>回主页</Button>
  </div>
}

export default UserProfile
